Given the staged git changes below, generate a Conventional Commits message in English.
Use `type: summary` as the first line. If there are additional items, list each on a separate line prefixed with `- `.
Return only the commit message text without explanations.

Change summary:
{changes}

Diff stats:
{diff}
